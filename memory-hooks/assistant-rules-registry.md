# Memory Hooks: Assistant Rules Registry

## Implementation Summary

```bash
python tools/memory.py save --title "AssistantRulesRegistry Implementation" \
  --tags agent,rules,system-instructions,registry,implementation \
  --notes "Centralized registry for managing all assistant rules with single update point, schema validation, and CLI interface"
```

## Architecture Components

```bash
python tools/memory.py save --title "AssistantRulesRegistry Components" \
  --tags architecture,components,registry,schema,cli \
  --notes "Core components: AssistantRulesRegistry class, RuleUpdateManager, AssistantRuleSchema, CLI interface, JSON Schema definition"
```

## Integration Points

```bash
python tools/memory.py save --title "AssistantRulesRegistry Integration" \
  --tags integration,extractAssistantPrompts,validation,cursorrules \
  --notes "Integration with extractAssistantPrompts.ts, .cursorrules file, prompt templates, and guidelines files"
```

## Next Steps

```bash
python tools/memory.py save --title "AssistantRulesRegistry Next Steps" \
  --tags next-steps,testing,documentation,web-interface,version-control \
  --notes "Next steps: integrate with extractAssistantPrompts.ts, add tests, enhance version control, create web interface, improve documentation"
```

## File Locations

```bash
python tools/memory.py save --title "AssistantRulesRegistry File Locations" \
  --tags files,location,structure,organization \
  --notes "Key files: src/assistant/AssistantRulesRegistry.ts, src/assistant/RuleUpdateManager.ts, src/assistant/AssistantRuleSchema.ts, src/schemas/assistant-rule.schema.yaml, src/cli/assistant-rules.ts"
```

## Usage Examples

```bash
python tools/memory.py save --title "AssistantRulesRegistry Usage" \
  --tags usage,examples,cli,api \
  --notes "CLI usage: npm run assistant-rules:view, npm run assistant-rules:update, npm run assistant-rules:add, npm run assistant-rules:remove, npm run assistant-rules:validate"
```
